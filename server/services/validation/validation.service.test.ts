import validation from './validation.service';

describe('validation service', () => {
  it('should be a function', () => {
    expect(typeof validation).toBe('function');
  });

  // Add more tests for schema validation
});
